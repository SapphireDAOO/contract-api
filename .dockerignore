# Secrets are supplied at runtime via compose env_file, never baked into a layer.
.env
.env.*

.git
.gitignore

# Bun installs inside the image; host modules are built for the wrong platform
# and would overwrite the ones the builder stage produced.
**/node_modules

# Local build output and tooling artifacts.
tmp/
api-test
.air.toml

Dockerfile
docker-compose.yml
README.md
.claude
